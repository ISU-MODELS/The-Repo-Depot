# =============================================================================
# Pytest configuration and shared fixtures
# =============================================================================
# Pytest auto-discovers this file when running tests in this directory. Use it
# to define fixtures (e.g. client, db session) that multiple test modules need.
# Fixtures defined here are available to all test_*.py files without importing.
# See: https://docs.pytest.org/en/stable/reference/fixtures.html
# =============================================================================

# No fixtures are required for the current tests. Add them here when you need
# shared setup, for example:
#
#   @pytest.fixture
#   def sample_user():
#       return {"id": 1, "name": "Test User"}
